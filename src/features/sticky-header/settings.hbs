{{ includes "@ta-x-partials/checkbox-setting.hbs" label="Sticky Header" id="chkStickyHeader" config-path="stickyHeader.enabled"
    helper-render-condition='{ "selector": "#chkStickyHeader", "value": "true" }'
    helper-text="This feature may cause some sticky elements to look buggy. Let me know what you spot!" }}

{{ includes "@ta-x-partials/checkbox-setting.hbs"
    label="Remain Stuck"
    id="chkStickyHeaderRemainStuck"
    config-path="stickyHeader.remainStuck" 
    render-condition='{ "selector": "#chkStickyHeader", "value": "true" }' }}